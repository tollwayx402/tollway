{
  "name": "@octroi/service-ingest",
  "private": true,
  "version": "0.1.0",
  "description": "Minimal Octroi cloud ingest + dashboard v1 (BSL).",
  "license": "BUSL-1.1",
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "node dist/main.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@octroi/core": "workspace:*",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@octroi/ingest": "workspace:*",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "@octroi/express": "workspace:*"
  }
}
